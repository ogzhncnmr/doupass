## What changed

## Why

Closes #

## Checks

- [ ] `go build ./... && go vet ./... && go test ./...` pass
- [ ] New behavior has positive and negative tests (rules: fixtures in `rules/cases.yml`)
- [ ] Policy format changes update `spec/policy-v0.md` and the schema together
- [ ] No comments added unless they explain a non-obvious decision
